import { expect } from 'chai';
import { pathObject } from '../../src/internal';

// TODO:JS->TS:INFO to be done
describe('pathObject', () => {

    it('can be initialized', () => {

        // const PathObject = new pathObject();
    });
});